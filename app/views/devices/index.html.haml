%h1 Listing devices

%table
  %tr
    %th Name
    %th Kind
    %th Product
    %th Site
    %th
    %th
    %th

  - @devices.each do |device|
    %tr
      %td= device.name
      %td= device.kind
      %td= device.product_id
      %td= device.site_id
      %td= link_to 'Show', device
      %td= link_to 'Edit', edit_device_path(device)
      %td= link_to 'Destroy', device, :confirm => 'Are you sure?', :method => :delete

%br

= link_to 'New Device', new_device_path
